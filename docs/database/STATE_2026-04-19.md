# SC Labs — Estado de la Base de Datos

**Fecha:** 2026-04-19
**Supabase Project Ref:** `htqfrcxtsghhcmimxdad`
**Game Version Target:** Star Citizen `4.7.1-live`
**Autor:** nightly audit nocturno (Claude + Pablo)

Este documento resume el estado de la capa de datos (migraciones + tablas + seeds) tras la noche del 2026-04-19, el commit de `Fase N.4 Mining Material Finder` y la creación de las migraciones 049/050.

## 1. Migraciones en `database/migrations/`

### Bloque estable 001–032 — Catálogo de componentes

Una migración por tabla. Cubre el catálogo de equipamiento/hardpoints ingestado desde `scunpacked` v1 y v2:

| # | Tabla | Notas |
|---|-------|-------|
| 001 | manufacturers | base — todas las demás FK apuntan acá |
| 002 | armors | |
| 003 | cargo_grids | |
| 004 | containers | |
| 005 | coolers | extendida por 041 |
| 006 | emps | |
| 007–010 | flair_* | cockpit / floor / surface / wall items |
| 011 | flight_controllers | extendida por 038 |
| 012–013 | fuel_intakes / fuel_tanks | |
| 014 | life_support_generators | |
| 015 | main_thrusters | seed pendiente (`_____`) |
| 016 | manneuver_thrusters | ⚠ typo en nombre de archivo (debería ser `maneuver`) — no podemos renombrar sin romper histórico, se fixea en próxima refactor |
| 017–018 | missiles / missile_launchers | |
| 019 | paints | seed pendiente (`_____`) |
| 020 | power_plants | extendida por 039 |
| 021–022 | quantum_drives / quantum_fuel_tanks | extendida por 042 |
| 023 | quantum_interdiction_generators | |
| **024** | — | **GAP** — no existe archivo 024 |
| 025 | scanners | ⚠ La migración fue creada pero el `CREATE TABLE` está incompleto o no aplicado. Task #12 pendiente. Seed marcado `__BUSCAR__` |
| 026 | self_destruct_systems | |
| 027 | shields | extendida por 040 |
| 028 | transponders | seed marcado `__BUSCAR__` |
| 029 | turrets | |
| 030 | weapon_attachments | seed marcado `__BUSCAR__` |
| 031 | weapon_defensives | |
| 032 | weapon_guns | extendida por 043, 045, 046. **104 de 180 filas** tienen `ammo_capacity=0` (task #10 pendiente) |

### Bloque 033–044 — Colisión de numeración (task #6)

Durante el desarrollo simultáneo del módulo power-model y del módulo mining surgió un conflicto: dos archivos reclaman los números 033–044.

**Migraciones power-model (cambio de esquema para DPS):**

| # | Archivo | Qué hace |
|---|---------|----------|
| 033 | `033_alter_ships_power_model.sql` | ALTER TABLE ships: agrega columnas power |
| 034 | `034_create_ship_power_reference.sql` | CREATE TABLE ship_power_reference |
| 035 | `035_create_ship_pools.sql` | CREATE TABLE ship_pools |
| 036 | `036_alter_ship_hardpoints_power_model.sql` | ALTER TABLE ship_hardpoints |
| 037 | `037_alter_radars_power_model.sql` | ALTER TABLE radars |
| 038 | `038_alter_flight_controllers_power_model.sql` | ALTER TABLE flight_controllers |
| 039 | `039_extend_power_plants_energy.sql` | ALTER TABLE power_plants |
| 040 | `040_extend_shields_energy.sql` | ALTER TABLE shields |
| 041 | `041_extend_coolers_energy.sql` | ALTER TABLE coolers |
| 042 | `042_extend_quantum_drives_energy.sql` | ALTER TABLE quantum_drives |
| 043 | `043_extend_weapon_guns_energy.sql` | ALTER TABLE weapon_guns |
| 044 | `044_create_weapon_mining.sql` | CREATE TABLE weapon_mining |

**Migraciones módulo mining / trade (colisionan con power-model):**

| # | Archivo | Qué hace |
|---|---------|----------|
| 033 | `033_create_mining_sessions.sql` | CREATE TABLE mining_sessions |
| 034 | `034_create_mining_members.sql` | CREATE TABLE mining_members |
| 035 | `035_create_mining_work_orders.sql` | CREATE TABLE mining_work_orders |
| 036 | `036_create_mining_inventory.sql` | CREATE TABLE mining_inventory |
| 037 | `037_create_mining_member_ledger.sql` | CREATE TABLE mining_member_ledger |
| 038 | `038_add_inventory_quality_location.sql` | ALTER mining_inventory (collision safe, idempotente) |
| 039 | `039_create_commodity_prices.sql` | CREATE TABLE commodity_prices |
| 040 | `040_enable_mining_realtime.sql` | ALTER publication supabase_realtime |
| 041 | `041_fix_mining_rls_party_members.sql` | POLICY cleanup |
| 042 | `042_add_quality_to_inventory.sql` | ALTER mining_inventory (idempotente) |
| 043 | `043_create_trade_work_orders.sql` | CREATE TABLE trade_work_orders |
| 044 | `044_add_avatar_to_trade_wo_participants.sql` | ALTER trade_wo_participants |

**Estado actual en Supabase prod (verificado por audit de sesiones anteriores):** ambas ramas se aplicaron pero por orden alfabético distinto. No hubo crash porque todos los archivos son idempotentes (`CREATE TABLE IF NOT EXISTS`, `ADD COLUMN IF NOT EXISTS`, `CREATE POLICY IF NOT EXISTS`, etc.).

**Plan de resolución (task #6, no blocking):** renumerar el bloque mining a 033b..044b o a 100..111, dejando el bloque power-model tal cual. Esto se puede hacer en un commit con `git mv` puro sin reaplicar nada en Supabase.

### Bloque 045–048 — Parches weapons + fases A/B/C/D trade+mining

| # | Archivo | Qué hace |
|---|---------|----------|
| 045 | `045_weapon_capacitor_fields.sql` | ALTER weapon_guns — energy_per_shot |
| 046 | `046_weapon_thermal_fields.sql` | ALTER weapon_guns — thermal fields |
| 046 | `046_create_mining_stop_distributions.sql` | **colisión extra**, CREATE TABLE mining_stop_distributions |
| 047 | `047_create_mining_pending_payouts.sql` | CREATE TABLE mining_pending_payouts |
| 048 | `048_create_mining_settlement_ledger.sql` | CREATE TABLE mining_settlement_ledger |
| — | `_apply_fase_d_to_supabase.sql` | helper manual para correr 046+047+048 juntos |

Los 046–048 de mining ya están aplicados a prod (task #75 completada).

### Bloque 049–050 — Nuevo (noche 2026-04-19)

| # | Archivo | Qué hace |
|---|---------|----------|
| 049 | `049_create_hangar_executive_schedule.sql` | CREATE TABLE hangar_executive_schedule (single-row config del ciclo 185/65) + seed |
| 050 | `050_create_mining_material_deposits.sql` | CREATE TABLEs: mining_materials, mining_material_signatures, mining_locations, mining_location_yields + 363 filas de seed |

**Estado:** archivos listos en repo, **no aplicados aún en Supabase prod**.

Motivo: Chrome MCP no puede leer el body de Supabase Studio (CSP bloquea). Pablo tiene que correrlos manualmente al arrancar el día — ambos son idempotentes (`CREATE TABLE IF NOT EXISTS` + `ON CONFLICT DO UPDATE/NOTHING`), así que se pueden correr varias veces sin romper nada.

Hasta que se apliquen:
- `/hangar-executive` funciona con fallback hardcodeado (ver `src/app/api/hangar-executive/route.ts`).
- `/mining` tab Buscador muestra el warning `fallbackWarn` de i18n.

### Legacy

Tres archivos pre-numeración:

- `legacy_001_satellite_tables.sql`
- `legacy_002_fix_crew.sql`
- `legacy_003_fix_hardpoints.sql`

Ya aplicados históricamente, se conservan para trazabilidad.

## 2. Seeds pendientes (task #7)

Archivos en `database/seeds/` con placeholder markers que indican data faltante:

- `scanners_seed___BUSCAR___.sql` — necesita data de scanners de scunpacked
- `transponders_seed___BUSCAR___.sql`
- `weapon_attachments_seed___BUSCAR.sql`
- `main_thrusters_seed_____.sql`
- `manneuver_thrusters_seed_____.sql`
- `paints_seed_____.sql`

Para la mayoría, la data debería estar en `scunpacked/ships/*/Loadouts` — se extrae con `scripts/ingest_v3.py` + scripts de extracción por tipo.

## 3. Tablas por módulo

### Combat / hardpoints

- `ships` — catálogo de naves, con power model columns (post-033)
- `ship_pools` — ⚠ **trampa**: la columna `max_size` es texto, no integer (ver `.auto-memory/sc_labs_data_layer.md`)
- `ship_hardpoints` — con columnas industriales (categoría, mining/salvage/tractor) tras 036
- `ship_power_reference` — datos de referencia para pips/power
- Catálogo componentes: `weapon_guns`, `missiles`, `missile_launchers`, `shields`, `power_plants`, `coolers`, `quantum_drives`, `quantum_fuel_tanks`, `quantum_interdiction_generators`, `emps`, `scanners` (incompleta), `turrets`, `weapon_attachments`, `weapon_defensives`, `weapon_mining`
- Stats agregadas: `radars`, `flight_controllers`, `life_support_generators`, `fuel_intakes`, `fuel_tanks`, `main_thrusters`, `manneuver_thrusters`, `self_destruct_systems`, `transponders`, `armors`, `cargo_grids`, `containers`, `paints`
- Flair: `flair_cockpit_items`, `flair_floor_items`, `flair_surface_items`, `flair_wall_items`

### Mining

- `mining_sessions` — sesiones de minería colaborativa
- `mining_members` — rooster + shares
- `mining_work_orders` — órdenes vigentes/completadas
- `mining_inventory` — stock refinado/no refinado por sesión
- `mining_member_ledger` — distribución histórica
- `mining_stop_distributions` — parcial por venta en ruta (Fase A/B)
- `mining_pending_payouts` — cobros pendientes por miembro (Fase D)
- `mining_settlement_ledger` — liquidación final + marcado pagado (Fase D)
- **Nuevo (050, pendiente):** `mining_materials`, `mining_material_signatures`, `mining_locations`, `mining_location_yields`

### Trade

- `trade_work_orders` — órdenes de venta
- `trade_wo_participants` — con avatar tras 044
- `commodity_prices` — cache UEX (se repuebla con `scripts/populate_prices.mjs`)

### Hangar Ejecutivo

- **Nuevo (049, pendiente):** `hangar_executive_schedule` (1 row de config)

## 4. Trampas conocidas

1. **`ship_pools.max_size` es TEXT** — en la sesión del DPS builder se descubrió que contenía cosas como `"Small"` en vez de integer. Siempre castear o usar un `CASE WHEN` antes de comparar.
2. **Tablas vacías en producción:** varias tablas del catálogo de componentes tienen schema pero 0 filas en prod porque los seeds `__BUSCAR__/_____` nunca se corrieron (tasks #7 y #10).
3. **`ammo_capacity=0` en weapon_guns:** 104/180 filas — rompe el cálculo de sustained DPS para armas balísticas hasta que se pueble.
4. **Migraciones duplicadas 033–044:** ver sección 1. Son idempotentes pero el orden real de aplicación en prod es alfabético por nombre de archivo.
5. **RLS activa en todo mining/trade/hangar_executive:** policies públicas de lectura, escritura con service role. Las API routes usan `src/lib/db.ts` (postgres client) que corre como service role.

## 5. Próximos pasos concretos

**Acción inmediata (Pablo):**

1. Ir a `https://supabase.com/dashboard/project/htqfrcxtsghhcmimxdad/sql/new`.
2. Pegar y correr `database/migrations/049_create_hangar_executive_schedule.sql`.
3. Pegar y correr `database/migrations/050_create_mining_material_deposits.sql`.
4. Verificar en `/hangar-executive` que el badge de source cambie a `db`.
5. Verificar en `/mining` > tab Buscador que carguen materiales (no el warning naranja).

**Tech debt pendiente (no urgente):**

- Renumerar colisión 033–044 mining → 100+ (task #6).
- Completar seeds faltantes `__BUSCAR__/_____` (task #7).
- Crear tabla scanners correctamente en migración 025 (task #12).
- Poblar ammo_capacity en weapon_guns (task #10).
- Actualizar `sc-labs-project-context.md` al estado real de abril 2026 (task #8).

---

Generado automáticamente por la sesión nocturna del 2026-04-19.
